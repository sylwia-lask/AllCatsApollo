export interface Cat {
  id: string;
  name: string;
  description: string;
  color: string;
  photoUrl: string;
  likes: number;
}

export interface CatListItem {
    id: string;
    name: string;
    color: string; 
    photoUrl: string;
    likes: number;
}
